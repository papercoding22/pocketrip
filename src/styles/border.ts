import {StyleSheet} from 'react-native';

// default border
const borderStyles = StyleSheet.create({
  defaultBorder: {
    borderWidth: 1,
    borderColor: '#000000',
    borderRadius: 5,
  },
});

export default borderStyles;
